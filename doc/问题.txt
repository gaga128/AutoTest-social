1、可变参数，传入字典格式，实现不同个数的可变参数拼装成一个请求。
2、log打印重复，若不重复则只有最后一条，中间还会有空白
3、找更好的出报告方式：目前打印控制台，同时写log。但log里对比起来比较不直观。
4、第一次运行结果保存excel作为预期
5、如何自动制作用例，目前手动填写excel，复杂，因此只能用作回归
6、没考虑excel中每条用例参数个数不同的情况：目前默认每个接口一个excel用例，同个excel用例中每条用例参数个数一致，若有缺少，则认为传空字符串作为参数值。